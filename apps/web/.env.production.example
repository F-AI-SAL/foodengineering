# Production env. Copy to .env.production and fill in values.
# Point to the gateway, not directly to services.
NEXT_PUBLIC_API_URL=https://gateway.example.com/api
NEXT_PUBLIC_WS_URL=wss://gateway.example.com/ws
